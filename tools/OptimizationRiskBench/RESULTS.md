# 负优化反例 2026-09-05

在我这台 13900K 加 3090 上跑的 没开游戏 就查几个一直怀疑会拖后腿的东西

## 工作集修剪

256 MiB 私有内存 修剪完再逐页摸一遍 0.92 ms 变成 50.74 ms 六万五千多次缺页 修完是要还的

## CPU 耗时识别

一个算完就 Sleep(1) 的假帧循环 旁边放个没关系的死算线程 RenderLane.TryIdentify 八次全认错 把死算线程当成关键线程 同核的时候把它提到最高 假帧循环四秒里一个周期都没跑完 分到两个核上就没事 p50 15.47 对 15.40 ms

## 纯策略

VerifyHold(45W, 41W, 98%, 96%) 是 true BoostPriorityTarget 饱和无 lane 回 Normal 有 lane 回 High CacheWarm.NvmeBlocks(true, true) 是 true 都对

## 第二轮 音频 缓存 档案

- 音频低延迟是真生效的 44100 Hz 默认 448 帧 10.16 ms 开了 128 帧 2.90 ms 关了回 448
- 缓存预热 前台 4 KiB 随机读 后台预读 512 MiB 开关各两次 IOPS 16893 到 18698 p99 78 到 83.5 μs 和抖动混在一起 看不出好坏
- 交接台架 Repeat 3 跑两次 138 条各挂 2 条和 1 条 挂在档案替换的 IOException 上 保存失败让交接 epoch 失效 保护是起作用了 但暴露出保存不稳
- 拿只读句柄占住档案 保存失败 原文件没坏 松开后同一实例还是不存 新实例能存

## 改了什么

- 缓存预热砍了 旧偏好启动时清 旧档案里的覆盖键加载时扔掉
- CPU 耗时候选提优改成默认关的手动实验项 CPU 饱和游戏回普通优先级 候选取消
- 工作集修剪要可用内存低于 4 GiB 且低于总量八分之一才动
- 档案替换碰到 Win32 错误 32 33 1175 退避重试四次 25 50 100 200 ms 重试完还不行就留旧档
- 改名 强制入口 单项覆盖 批量入库 没存成功不发布
- 受限 CPU 域回 Normal 候选线程晋升撤了 功耗让路过滤无效采样 自动显卡分配按可见 EXE 和后代路径保护 共享显存查询改成单任务后台采样

改完 全量自测 66 套件全过 OptimizationSafety 扩到 24 组 交接台架两次 138/138 -StoreOnly 全过
