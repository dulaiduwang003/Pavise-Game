// @author bdth 2074055628@qq.com
// 文件用途 维护内置的三语版本说明并记录已读版本

using System;
using System.Collections.Generic;

namespace PaviseApp
{
    internal sealed class ReleaseNote
    {
        public readonly string Version;
        public readonly string Date;
        private readonly string[][] items;

        public ReleaseNote(string version, string date, string[][] entries)
        {
            Version = version; Date = date; items = entries;
        }

        public string Tag { get { return "v" + Version; } }

        public int Count { get { return items == null ? 0 : items.Length; } }

        public string Item(int index)
        {
            if (items == null || index < 0 || index >= items.Length) return "";
            string[] row = items[index];
            if (row == null || row.Length == 0) return "";
            int lang = Lang.Cur;
            if (lang < 0 || lang >= row.Length || string.IsNullOrEmpty(row[lang])) return row[0];
            return row[lang];
        }
    }

    internal static class ReleaseNotes
    {
        private const string SeenKey = "LastSeenNotesVersion";

        public static readonly ReleaseNote[] All = new[]
        {
            new ReleaseNote("1.6.4", "2026-08-05", new[]
            {
                new[]{ "系统体检新增六项检查：主屏刷新率（当前档位与同分辨率下的最高档，不匹配直接进结论清单——这是本页收益最直接且零代价的一项）、内存总量与余量、笔记本供电方式、后台占用前三的进程、Game DVR 后台录制状态、操作系统版本（含注册表把 Win11 写成 Win10 的纠正）。" },
                new[]{ "新增启动时的反馈弹窗：作者微信与 QQ 交流群，群号可一键复制，并写明本软件完全免费、买到即被骗。仅在你主动打开界面的那次启动弹出，开机自启进托盘不打扰；可勾选不再显示，设置页能重新开启。" },
                new[]{ "新版本启动时会接管仍在运行的旧版本：比对文件版本，只有严格更新的构建才会请旧实例退出，走的是它自己的退出流程，会完整还原进程调度与系统设置后再退出，不是强杀；等待超时则放弃接管而不冒险。版本相同时保持原行为，只把已有窗口唤到前台。" },
                new[]{ "修复系统体检页滚动之后重新体检，列表顶部会多出一段空白：AutoScroll 容器会把滚动偏移加到新控件坐标上，重建前需要先归位。同时修复重建时在遍历中释放控件导致只清理了一半、其余泄漏的问题。" },
                new[]{ "内置自测扩充到 111 项：新增滚动后重建回到顶部，以及版本比对只让严格更新的构建接管的用例。" },
            }),
            new ReleaseNote("1.6.3", "2026-08-05", new[]
            {
                new[]{ "新增「系统体检」页（硬件与系统分组）：只读聚合四组信息——写入能力（NVIDIA 驱动接口、CPU Sets 分区、效率模式）、本机实测（CPU 拓扑、整机占用、中断分布）、持久系统设置（HAGS、VBS、游戏模式、MPO、电源计划），以及一份结论清单。每条结论都标注依据等级：本机实测 / 台架实测 / 机制明确 / 未验证——没测过的就写没测过。" },
                new[]{ "体检页提供 NVIDIA 写入实测：对专用探测 Profile 做一轮写入-回读-还原，验证本机驱动是否真的接受深度调优的三项写入，而不是只看写入调用有没有报错。开发机 RTX 3090 上三项全部回读确认生效。" },
                new[]{ "移除 v1.6.2 的「中断核规避」。它每轮都能稳定选中正确的核，但继续评估量级后结论变了：中断的单次干扰是微秒级，2%~3% 的常态占用不足以造成可感知卡顿，而让出一个物理核的代价是确定的（混合架构上是 12.5% 的 P 核）。用确定的代价换大概率落在噪声里的收益，不成立。原 4% 风暴阈值的 DPC 避让是对的设计，保持不变。--irq-map 诊断保留，体检页内置其快速版。" },
                new[]{ "修复 Xbox / 微软商店版游戏（如极限竞速：地平线 5）无法加入目标库的问题。这类游戏装在 WindowsApps、XboxGames 一类目录下，那里的 ACL 连管理员都拒绝读取 EXE，于是两条添加路径同时失败：文件选择框自身的存在性校验会去打开文件，直接弹出系统级「没有打开该文件的权限」；「从运行进程添加」则卡在 PE 头校验上，因为读不到文件内容而被判定为无效 EXE。现在文件确实存在但内容读不到时，以存在为准放行，不再要求能读出 PE 头；不存在的路径和非 EXE 文件仍然拒绝。运行时的进程识别本就不受影响——路径来自 QueryFullProcessImageName，不需要读取文件。" },
                new[]{ "内置自测调整到 109 项：移除随功能删除的探测器用例，新增体检分层阈值与依据标注完整性、以及拒绝读取的 EXE 仍可加入目标库的用例（用 ACL Deny 真实复现商店目录的权限形态）。" },
            }),
            new ReleaseNote("1.6.2", "2026-08-05", new[]
            {
                new[]{ "新增「中断核规避」（默认关，需严格 CPU 分区或竞技模式，8 核以上生效）：网卡、USB 控制器等设备的中断和 DPC 会集中落在少数几个核上，游戏线程被调度到那里就得排队等中断处理完。对局开始后测量一次各核的中断占用，若某个物理核明显离群，就把它移出游戏分区，最多让出一个核。" },
                new[]{ "这一项没有按流传的做法固定屏蔽 CPU 0。开发机实测的结论正相反：中断稳定压在物理核 4/5（占用 1.7% ~ 2.9%），而承载 CPU 0 的核只有 0.05% ~ 0.10%，是最脏核的百分之二到四。现代设备通过 MSI-X 分散中断，承载中断的核由机器决定，按位置假定就会躲错核——真正脏的核仍留在游戏分区内。因此规避目标一律来自实测。" },
                new[]{ "修正中断规避够不到实际负载的问题。此前只有 DPC 瞬时风暴（占用 4% 以上）才会触发避让，而开发机上最脏的核只有 2.9%，永远够不着。中断占用按时钟中断周期累加，3 秒窗口的分辨率是 0.52%，低于此值一律显示为零——原判定读到的其实大多是量化噪声。新增的稳态判定改用 30 秒窗口测量，分辨率提高到 0.05%，因此规避在对局开始约半分钟后才生效。" },
                new[]{ "许可协议变更：项目不再使用 GNU GPL v3，改用 Pavise 许可协议。源码依然公开，任何人都可以免费使用、复制、修改和再分发，个人、公司、网吧均可，但禁止以任何形式收费分发——包括出售拷贝、付费下载、付费解锁、并入收费产品、预装到出售的整机。自愿赞赏不受影响，前提是给不给钱都不影响拿到软件和功能。「Pavise」名称与图标不在授权范围内，修改版须用自己的名称发布。" },
                new[]{ "内置自测扩充到 106 项，新增中断核判定的离群、并列、核数下限、SMT 峰值取值边界，以及探测器每会话只测一次、重置后可重测、绝不交出游戏分区外的核这三条不变量。" },
            }),
            new ReleaseNote("1.6.1", "2026-08-04", new[]
            {
                new[]{ "修复自解压式程序的真身被当作后台压制的问题。部分程序（如 Geek Uninstaller）会把真正运行的主体释放到临时目录，它不在你添加的安装目录下，此前拿不到游戏家族资格，结果被压到最低优先级并进入效率模式——等于把游戏主体按在地上，只有那个空壳启动器被提优。现在游戏进程的整棵后代进程树都会被豁免，无论它跑在哪个目录。该豁免只用于免除压制，不参与提优目标的选择。" },
                new[]{ "新增写入失败熔断：任何带界面开关的优化项，连续 2 次写入失败就自动关闭对应开关并写明日志，不再无限重试刷屏；重新打开开关即恢复尝试。覆盖全部环境项、电源计划与 NVIDIA 驱动调优。电源计划失败重试同时改为退避（30 秒起步、封顶 5 分钟），被第三方电源软件接管的机器不再每 40 秒刷两行失败日志。" },
                new[]{ "识别自保护进程：对拒绝全部策略写入且经核验从未被改动的进程（火绒等 HIPS 类安全软件），按句柄受保护处理并记入免压制名单，后续对局直接跳过，不再留下无意义的还原快照反复重试；上次遗留的此类记录会在启动时自动清除。「恢复已记录调度」可清空名单重新探测。" },
                new[]{ "游戏退出增加 15 秒还原宽限期：启动器换壳、秒级重启不再触发整套还原又整套重开，刷新率和电源计划不再来回切换。" },
                new[]{ "竞技电源策略补齐隐藏调速器参数（机器支持才写入）：最大处理器状态显式 100%、升频策略 Rocket 即时拉满、降频策略逐级缓退、延迟敏感提示全速、混合架构 E 核不再停核。" },
                new[]{ "游戏提优现在会把「退出效率模式」纳入验证并写进日志。此前这一项既不在验证范围内，成功与否也没有任何记录，而效率模式会压低频率、让调度器把进程当省电任务处理，足以抵消高优先级带来的收益。现在写入后一律回读确认，日志会明确显示已退出或未清除。" },
                new[]{ "移除 NVIDIA 低延迟中一项驱动拒绝写入的设置。该项只决定控制面板把选项显示成什么，不改变驱动行为，真正生效的预渲染队列限制不受影响，日志不再出现相应的写入失败记录；其余驱动写入失败的日志现在附带 NVAPI 状态码。" },
                new[]{ "证据模式开启前增加确认弹窗，明示采集本身有开销、可能轻微影响帧率、仅供测试与排查使用。" },
                new[]{ "日志措辞修正：渲染主权域采样失败注明进程与启动器场景、刷新率被游戏自行切回时明示无需还原、解除统计注明为本局累计。" },
                new[]{ "内置自测扩充到 101 项，新增后代豁免边界、效率模式回路、自保护进程判定与免压制名单的用例。" },
            }),
            new ReleaseNote("1.6.0", "2026-08-02", new[]
            {
                new[]{ "项目仓库已迁移：新地址为 github.com/dulaiduwang003/Pavise-Game，本版起「检查更新」自动指向新仓库，旧仓库不再发布新版本。" },
                new[]{ "新增「后台冻结」（默认关，仅竞技 / 激进自定义档）：对已隔离、连续 30 秒零占用且没有任何可见窗口的后台进程执行挂起，彻底交出 CPU 与调度资源。反作弊、游戏本体及其家族、白名单、系统核心与其它用户会话永不冻结；退出游戏、一键恢复、关闭开关、离开竞技档都会立即解冻，程序崩溃后下次启动自动唤醒。开启前会完整告知代价——被冻进程不响应网络、不能保存、只有托盘图标的程序在冻结期间点不动。" },
                new[]{ "新增「渲染主权域」（默认关）：进程级提优把游戏上百个线程一视同仁，真正决定帧何时呈现的往往只有一个。开局识别出 CPU 主导线程后单独抬高一档，游戏内部再抢也先保帧路径，对资源加载与着色器编译引起的自家卡顿有效。识别只在开局做一次，对局中不再枚举线程；负载分散在多线程、或游戏已自行调好线程权重时自动跳过不介入。" },
                new[]{ "新增「NVIDIA 低延迟」（默认关，显卡页）：按游戏写驱动 Profile，把渲染队列从默认的 2-3 帧压到 1 帧。GPU 占用打满时可观降低系统延迟；CPU 瓶颈或已限帧的场景没有收益，请按实际情况开启。不注入游戏、不改画质，原值有快照，关闭即恢复。" },
                new[]{ "新增「后备提优」（默认关）：游戏进程被反作弊保护、打不开句柄时，改由 IFEO 让系统在进程创建时直接赋予高优先级，绕开句柄限制。只写优先级一项，不碰任何调试劫持向量；关闭开关会把注册表痕迹清理干净。" },
                new[]{ "新增「Windows 游戏模式守护」与「TCP 低延迟」（均默认关，系统环境页）：前者把被旧优化教程关掉的系统游戏模式拨回开启；后者逐网卡禁用 Nagle 合包与延迟 ACK，只对走 TCP 的游戏有效，MOBA / FPS 等 UDP 对局无收益，文案已如实标注。" },
                new[]{ "修正「前台调度加权」：此前写入的 Win32PrioritySeparation 值解析后与系统默认行为等价，实为空操作。现改为固定量子，作用是防止游戏满载时语音、推流线程被饿死，名称与说明同步更正为「前台调度稳定」。" },
                new[]{ "「后台 GPU 让位」与「后台冻结」开关此前只存在于一个从未接入界面的旧对话框中，用户实际无法开启，现已接入优化策略页。同时修正「网络优化」的说明：该项只影响多媒体网络限流，对多数游戏没有实际作用。" },
                new[]{ "自测用例扩充到 93 项，新增驱动设置键位映射、网卡清单编解码、IFEO 沙箱零残留、帧关键线程识别等用例。" },
            }),
            new ReleaseNote("1.5.1", "2026-07-31", new[]
            {
                new[]{ "新增「后台 GPU 让位」开关（默认关闭，自定义策略内）：重压 / 隔离档的后台进程连 GPU 调度优先级一并压低，GPU 吃紧时后台渲染与转码让位给游戏；原值随压制快照记录，恢复与崩溃自愈走同一条路。直播推流等需要后台继续工作的软件请先加入白名单。" },
                new[]{ "帧率证据剔除失焦帧：游戏在后台主动降帧的部分不再污染平均帧率与 1% / 0.1% Low，证据行单独报告剔除的失焦时长与占比；对焦帧不足时明示本局无法给出可信统计。切屏前后跨越边界的帧一并剔除。" },
                new[]{ "游戏库互保：竞技模式不再把游戏库里另一个正在运行的游戏当作后台进程压制，双开挂机不再被误伤；提优与逐游戏设置仍只服务当前检测到的会话。" },
                new[]{ "英雄联盟安装扫描改为按需：安装位置未知或失效（卸载、移动）时，启动不再自动扫描——进入专栏页、打开专栏开关、执行专栏操作或相关进程出现时才会扫；扫描遮罩只覆盖专栏页、可手动取消，扫描期间锁定模式切换；已定位的安装后台校验完全静默，不再做全盘探测。" },
                new[]{ "预设白名单收敛为系统核心进程，不再预置任何第三方软件；例外由用户在白名单页自行添加。" },
                new[]{ "一次性白名单清理：本版本起预设白名单收敛为系统核心；早前构建并入的 11 条第三方豁免会在下次启动时自动移除，用户此后自行添加的例外不受影响。游戏库互保同时拒绝盘符根档案路径，避免整个磁盘被豁免压制。开机自启迁移拒绝临时/微信缓存等易失目录。" },
                new[]{ "修复压制自测的一个竞速误报，并新增 GPU 让位、失焦剔除与游戏库互保的自测；自测用例扩充到 82 项。" },
            }),
            new ReleaseNote("1.5", "2026-07-29", new[]
            {
                new[]{ "新增「英雄联盟专栏」：WeGame 只负责认证与拉起，LCU 确认大厅可用后按安装路径精确退出国服附加进程；对局中通过客户端原生接口关闭 CEF/UX，赛后自动回显，独立看门狗在 Pavise 退出后仍负责恢复。" },
                new[]{ "新增附加层直接删除：单独确认后删除 Cross 附加层（AI 教练、iCreate 录制），客户端更新会重新下载这些组件；客户端、WeGame 或反作弊运行时拒绝操作，删除范围不含游戏本体、登录链路与更新器。" },
                new[]{ "新增「竞技画质」：一组可逆的 game.cfg 设置（阴影、抗锯齿、光束、装饰效果、画质档位、独占全屏），原值连同安装路径一并记录，可一键还原。" },
                new[]{ "界面仅保留简体中文，构建体积由约 610 KB 降至约 500 KB；多语言机制与全部文案键保留，补回译文即可恢复。" },
                new[]{ "后台压制与提优的一批稳定性修复：EcoQoS 异步生效导致的误报、把自愿进入后台的进程误判为残留并反向提权、陈旧条目导致运行中的游戏被反复拆装、恢复日志中进程名解码失败被静默丢弃。" },
                new[]{ "英雄联盟运行时加固：LCU 凭据的日志来源改为校验端口属主进程；WeGame 根拒绝卷根；看门狗就绪判定不再仅凭命名对象；净化判定权归路径，此前两个已声明的清理目录从未生效。" },
                new[]{ "退出、关机与崩溃恢复修正，以及单处理器组 L3、非对称缓存处理器、Windows 10 EcoQoS 读取等系统接口修正。" },
                new[]{ "会话报告改为分行展示，解决等宽字体下中文双倍宽导致的折行截断；磁盘格式不变。" },
            }),
            new ReleaseNote("1.4.4", "2026-07-25", new[]
            {
                new[]{ "完成原有通用优化功能线；后续英雄联盟专项能力从 1.5 开始独立演进。" },
                new[]{ "安全修复：后台压制的反作弊豁免此前用的是精确名单，而检测器用的是含子串的宽判定。两者宽度不一致时，程序自己认定为反作弊的进程仍可能被压制——被压的反作弊心跳超时可能导致掉线。现已统一为同一套判定，并在冻结路径上加了独立的第二道拦截。" },
                new[]{ "安全修复：向 PowerShell 传递目录与策略名时使用字符串拼接。PowerShell 也把排版引号（U+2019 等）当作定界符，因此转义 ASCII 单引号并不足够；本程序以管理员身份运行，构造特定名称的目录可导致以管理员权限执行任意命令。现改为经环境变量传参、脚本以 -EncodedCommand 传入且不再落临时文件。" },
                new[]{ "修复：游戏档案文件读取失败时会被当成「档案为空」，界面显示空游戏库，此时新增任何一个游戏都会把原档案整份覆盖。现在读取失败会被识别并拒绝保存，原文件受到保护。" },
                new[]{ "修复：主界面部分说明文字因换行条件从不成立而被截断，被截掉的往往正是风险提示；托盘菜单文字偏上；游戏库列表与标题栏在重复绘制时可能因共享字体被释放而反复报错。" },
                new[]{ "还原可靠性：进程原有的 EcoQoS 设置、逐游戏图形选项中由 Windows 写入的其它字段、中断亲和改动过的设备名单，现在都会被完整保留并还原，不再被覆盖或遗漏。" },
                new[]{ "本次共修复 26 个问题，自测用例从 39 项扩充到 59 项。" },
            }),
            new ReleaseNote("1.4.3", "2026-07-25", new[]
            {
                new[]{ "GPU 中断亲和优化：把显卡中断引导到游戏所在的核心附近，减少跨核/跨缓存开销。多适配器或多处理器组系统只应用较轻的邻近策略。需重启显卡设备或电脑后生效。" },
                new[]{ "游戏网络优先：同样把网卡中断引导到游戏核心附近，并给游戏目录中已添加的可执行文件的流量打上 QoS 优先级标记，减少排队和抖动。" },
                new[]{ "压制豁免不再写死平台名：改为沿当前游戏渲染进程的父进程链向上识别启动器宿主，任何平台、任何游戏都自动适用；进程树够不到的常驻启动器外壳则按通用启动器类别在对局期间兜底豁免。" },
                new[]{ "新增开场动画：主窗口打开时淡入并轻微上浮，不再生硬地直接弹出。" },
                new[]{ "新增「检测到游戏后自动收起窗口」开关（默认关闭）：检测到游戏 10 秒后把主窗口收回托盘，每局只收一次，之后手动打开不会再被收走。" },
                new[]{ "新增版本说明：程序内置三语更新说明，离线可查看；升级到新版本后第一次打开会有未读标记。" },
                new[]{ "修复：托盘右键菜单文字整体偏上约 6~7 像素，现已垂直居中。" },
                new[]{ "修复：注册表恢复机制此前无法正确处理二进制类型的值，比较时会退化成类型名比较而非内容比较。" },
            }),
            new ReleaseNote("1.4.2", "2026-07-24", new[]
            {
                new[]{ "任意进程加速：不再局限于游戏，你手动添加的任何程序都能被识别、保护并提速，进程一跑起来就生效，不需要它有可见窗口或处于前台。" },
                new[]{ "套壳启动器识别：把真正的主程序解压到临时目录再运行的启动器，现在也能被正确认出（app → app64 / app_x64 / app-v2 这类位数或版本后缀），同时拒绝 app → app_updater 这种会误伤第三方进程的匹配。" },
                new[]{ "粘性会话保护：一局确认之后，alt-tab 切到桌面或别的程序都不会再被误判成目标已退出，保护和加速会一直保持。" },
                new[]{ "竞技级压制范围：竞技模式（以及自定义模式下新增的同名开关）不再豁免前台和可见窗口，除真正的 Windows 核心服务外一律压制。" },
                new[]{ "核心系统服务豁免收紧：从原来的「C:\\Windows 下一律不动」改成按进程名加安装路径双重匹配的精确白名单，只在竞技级强度下生效。" },
                new[]{ "修复：竞技模式下 alt-tab 离开游戏，曾会导致游戏进程自己被当成后台压制，在有反作弊的游戏里可能掉线。" },
                new[]{ "修复：位数/版本后缀匹配过去允许任意后续内容，可能让名字前缀撞车的无关第三方进程被当成你选定的目标而获得完全信任。" },
                new[]{ "安全：反作弊进程无条件豁免压制，且改为按进程名加已知子串双重匹配，堵上「内置目录里没收录的反作弊变体可能被压制」这个缺口。" },
            }),
            new ReleaseNote("1.0", "2026-07-24", new[]
            {
                new[]{ "本仓库下的首个公开发布版本。" },
            }),
        };

        public static ReleaseNote Current
        {
            get
            {
                foreach (ReleaseNote n in All)
                    if (string.Equals(n.Version, App.Version, StringComparison.OrdinalIgnoreCase)) return n;
                return null;
            }
        }

        public static bool HasUnseen
        {
            get { return !string.Equals(Settings.LoadStr(SeenKey, ""), App.Version, StringComparison.OrdinalIgnoreCase); }
        }

        public static void MarkSeen() { Settings.SaveStr(SeenKey, App.Version); }

#if PAVISE_SELFTEST
        internal static List<string> MissingTranslations()
        {
            var bad = new List<string>();
            foreach (ReleaseNote n in All)
                for (int i = 0; i < n.Count; i++)
                {
                    int prev = Lang.Cur;
                    try
                    {
                        for (int lang = 0; lang < 3; lang++)
                        {
                            Lang.Cur = lang;
                            if (string.IsNullOrEmpty(n.Item(i))) bad.Add(n.Version + " #" + i + " lang" + lang);
                        }
                    }
                    finally { Lang.Cur = prev; }
                }
            return bad;
        }
#endif
    }
}
