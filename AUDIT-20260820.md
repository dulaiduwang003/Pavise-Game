# Pavise 台架审查报告

**日期** 2026-08-20 · **版本** v1.8.1.2 · **代码规模** 197 文件 / 51,799 行 · **状态** 审查 + 修复已完成并复核

**台架机器** i7-9750H 6C/12T · RTX 2070 Max-Q · Windows 11 26200

**方法** 四个模型（deepseek-v4-pro / glm-5.2 / qwen3.8-max / kimi-k2.6）做静态审查生成假设，用项目自带 StressLab 在开发机上实测裁决。凡代码注释、README 或模型结论与实测冲突的，一律以实测为准。

---

## 四项处置结果

| # | 事项 | 处置 |
|---|---|---|
| 01 | 挤核门槛 `SqueezeMinPhysical = 7` | **已改回 5**，注释换成三轮实测数据。本机复核 `SqueezeSupported=True`、`BackgroundSqueezeMask=0xC00`，正是实测 +75~85% 那一档。另加迁移把被 1.8.1.0 误关的开关恢复（本机已从 0 变 1） |
| 02 | 修复型功能转自动执行 | **已做，收 2 项**（NetTweak / InputMythTweak）。提权复核：`NetworkThrottlingIndex` 由 `0xFFFFFFFF` 自动修为 `0xA`，快照 `=-1=10` 完整可撤销。`PlatformClockTweak`（写 BCD）、`MsiModeTweak`（改设备中断模式、需重启）、`FthTweak`（有权衡）按门槛排除，仍留体检页 |
| 03 | 中断亲和清除顺序 | **已修三处**：`LegacyPurge` 改 LIFO、`OwnedByOtherTweaks()` 纳入三代退役台账、迁移等在役功能先还原 |
| 04 | 退役壳合并 | **部分做**：三个亲和壳收敛到统一台账表（同时是 03 的数据源）。`IfeoBoost+CfgOffTweak`、`SvcPause+UpdatePause` 两组未动——它们不修复任何已证实缺陷，纯重构的回归风险大于收益 |

**复核**：主程序与自测双双编译通过；自测 257 项 **248 PASS / 0 FAIL / 9 SKIP**。

## 一、一个被误杀的核心功能

`SqueezeMinPhysical = 7`（`src/Platform/CpuPartitionPolicy.cs:25`）把 6 核及以下机器排除在「把重压后台挤进少数核」之外。引入它的提交是 v1.8.1.0「后台资源牢笼」，commit message 通篇讲 Job 对象限速，只字未提这个门槛。当时随改动写下的理由是（这句注释在后续提交里又被删掉，改动前的代码只剩一个没有任何说明的 `= 7`）：

```diff
- public const int SqueezeMinPhysical = 5;
+ // 6 核及以下实测会因后台热点集中反伤前台，硬门槛不允许配置绕过。
+ public const int SqueezeMinPhysical = 7;
```

**这句「实测」在仓库里找不到任何数据支撑。** docs/ 全是截图没有记录；而唯一存在的 6 核挤核实测（`tools/StressLab/README.md`，作者本人在这台机器上做的）结论正好相反：挤核关 33.3 fps，挤核开 95.4 fps。

### 我跑的三轮

压力源用 README 认定的杀手场景（10 线程内存带宽争抢），渲染器用同一份假游戏参数（关键路径 6ms + 24MB 工作集 + 3 辅助线程，不限帧）。挤核掩码 `0xC00` 就是产品在 6 核上 `reserve=1` 会算出来的值。

**A/B 复现**

| | 40s 不挤核 | 40s 挤核 | 180s 不挤核 | 180s 挤核 |
|---|---:|---:|---:|---:|
| 平均帧率 | 43.71 | **77.06** | 39.10 | **68.57** |
| 1% low | 39.45 | **56.68** | 35.04 | **51.80** |
| p50 帧时间 | 23.04 ms | **12.65 ms** | 25.66 ms | **14.30 ms** |
| p99 帧时间 | 25.03 ms | **17.15 ms** | 28.15 ms | **18.69 ms** |
| 最长帧 | 26.02 ms | **19.28 ms** | 31.01 ms | **21.34 ms** |
| CPU 性能% 后半程 | 153.3 | 153.1 | 153.1 | 153.2 |

CPU 性能% 取自 `Win32_PerfFormattedData_Counters_ProcessorInformation`，149 个采样点，最低值全部是 153，无一次掉落。

**D3D 呈现路径复核**（新增 `tools/D3DLab`，真 D3D11 设备 + 真 DXGI 交换链 + 真 `Present`，PresentMon 从系统 ETW 独立取证）

| 40 秒/臂 | 不挤核 | 挤核 `0xC00` | 变化 |
|---|---:|---:|---|
| 渲染器自测 fps | 85.67 | **194.92** | +128% |
| PresentMon(ETW) fps | 85.51 | **193.60** | +126% |
| 自测 / ETW p50 | 11.625 / 11.666 ms | 5.046 / 5.073 ms | −57% |
| 自测 / ETW p99 | 14.208 / 14.335 ms | 6.287 / 6.398 ms | −56% |
| 自测 / ETW 1% low | 67.01 / 66.32 | **153.74 / 149.11** | +126% |

两路独立测量差距 < 0.4%，ETW 侧分别采到 4445 / 10066 个 Present 事件。**GDI 台架给 +76%，D3D 呈现路径给 +126%**——方向一致、幅度更大，收益不是 GDI 渲染路径的特例。

**拐点扫描**（后台允许铺多宽，40 秒／点）

| 后台可用 | 掩码 | 平均帧率 | 1% low | p99 |
|---|---|---:|---:|---:|
| 1 物理核 | `0xC00` | **69.11** | **52.03** | **18.78 ms** |
| 2 物理核 | `0xF00` | 54.92 | 44.65 | 22.06 ms |
| 3 物理核 | `0xFC0` | 46.11 | 37.99 | 25.26 ms |
| 不限制 | `0xFFF` | 37.25 | 32.90 | 30.19 ms |

### 三条结论

1. **挤核在 6 核上收益 +75~85%**［实测］ — 全部帧时间分位数改善，无一项恶化。40 秒和 180 秒双测方向一致，重复性好（`0xC00` 两次独立运行是 68.57 和 69.11，极差 < 1 fps）。
2. **「热点集中导致降频」不存在**［实测］ — 两臂 CPU 性能% 完全锁在 153，长测后半程也不掉，挤核臂甚至略高。180 秒是专门为堵「短测看不出热累积」跑的。
3. **曲线单调，最窄最优**［实测］ — 后台越集中前台越好，一路单调无膝盖。**这直接反证了「后台热点集中反伤前台」的机制本身**：如果热点集中有害，最窄的 `0xC00` 应该最差，实测它最好。

### 封锁有多彻底

- UI：`swCoreSqueeze.Enabled = !permanent`，6 核判定为 `SqueezeTooFewCores`，开关禁用并强制置 false
- 逻辑层：6 处 `CpuTopology.SqueezeSupported &&` 门控（`GameMode.cs`、`GameMode.Settings.cs`、`PolicyResolver.cs` 等）
- 逐游戏配置：`GameConfigPage.cs:352` 直接返回 false
- v1.8.1.0 迁移主动关掉老用户的开关，本机 `Reset_GmSqueezeBgSmallCpu_1810=1`、`GmSqueezeBg=0`

**一个让结论更硬的确认**：产品的挤核走 `Native.SetProcessAffinityMask` 硬亲和（`SuppressionCore.Apply.cs:170`），CPU Sets 软亲和只用在「分区」那条路径。台架用的就是硬亲和，和产品行为完全一致，没有软硬之差的折扣。

---

## 二、该转成自动执行的五项

> **处置**：已收录 `NetTweak`、`InputMythTweak` 进开机自动校正（`AutoRepair.cs`）。收录门槛四条全中才进：回系统默认、幂等、不需重启、不碰启动配置与设备参数。`PlatformClockTweak`（写 BCD）、`MsiModeTweak`（改设备中断模式且需重启）、`FthTweak`（摘容错堆是权衡不是回默认）按门槛排除，仍留体检页手动。自动修过的项在体检页依旧可撤销。

共同点是「把被改坏的值改回系统默认」——幂等、无权衡、不需要会话生命周期。唯一入口都是体检页的 Fix 按钮，无任何自动路径。

| 功能 | 实际动作 | 本机实测状态 |
|---|---|---|
| `NetTweak` | NetworkThrottlingIndex 回默认 10 | **`0xffffffff` 坏值，至今未修** |
| `InputMythTweak` | 鼠键队列长度回默认 | 纠正第三方工具改动 |
| `PlatformClockTweak` | 删 bcdedit 时钟覆盖 | 纠正老教程改动 |
| `GameModeGuard` | AutoGameModeEnabled 回 1 | 本机已是 1，no-op |
| `MsiModeTweak` | 只修 `MSISupported=0` 的设备 | 两卡均为 1，**彻底 no-op** |

**铁证**：开发机上 `NetworkThrottlingIndex` 到现在还是 `0xffffffff`。`NetTweak.NeedsRepair()` 判据是 `< 1 || > 70`，该值读成 int 是 −1，妥妥命中。逻辑完全正确，功能从来没生效过——因为连作者都不会去点那个按钮。

**一个例外**：`FthTweak` 不建议自动化。FTH 是防堆损坏崩溃的安全网，摘掉会让真有堆问题的游戏直接崩，属于有权衡，保留开关。

---

## 三、Bug 清单

二弟报了 8 条。**抽验 4 条，全部不成立**——2 条是明显误读，另 2 条深入验证后同样站不住。静态审查在这个库上的假阳性率很高。

### 深入验证后推翻的两条

**`WlanGuard.Restore()` 吞掉还原失败** — 报告初版列为严重 bug，**实测推翻**。

写了 P/Invoke 探针在 Intel Wireless-AC 9560 上实测：同句柄写 TRUE 读回 1 → `WlanCloseHandle` → 新句柄读回 0。**媒体流模式随客户端句柄关闭自动失效**，`WlanCloseHandle` 本身已完成还原，后面那次显式写 FALSE 只是让它早一点生效。所以即便它失败，网卡也不会留在媒体流模式，`return true` 是正确的。（六弟当时判断"会持久保留"，它自己标注了"没在官方文档找到明确语句"——实测证明它推错了。）

**`NvDrsTweaks.RestoreKind()` 静默早退** — 报告初版说"快照永久残留、零日志"，**复查推翻**。

`RestoreKind` 早退后，`HealOrphans` 的循环里 `HasSnapshotFor` 仍为真 → `stuck++` → 落到日志「NVIDIA 驱动调优 N 类残留快照暂时无法还原 下次启动继续尝试」。快照保留是正确行为（驱动不可用时不能丢弃），每次启动都会重试，驱动恢复后即可还原。既不是永久残留，也不是零日志。

### 明显误报的两条

**`DisplayAwake` 状态不一致** — 二弟说失败时 `active` 已置 false 导致下次跳过。实际 `return false` 在 `active = false` **之前**，失败时 active 保持 true，下次会重试。顺序说反了。

**`PowerPlan.CorePark` 快照从未保存** — 二弟以为在改用户的电源方案。实际 Pavise 创建自己的托管方案（本机 `PgPlanGuid=053a7443-…`），所有 Knob 写进自建方案，退局切回 `PrevPowerPlan`，用户原方案根本没被碰，本就不需要快照。

### 未验证的四条

SuppressionCore 损坏日志行会累积、`GameMode.SyncGameDvr/SyncMmcss` 的空 catch 吞异常、`RestoreParkState()` 返回值被忽略、`ReversibleReg.Restore` 检测到外部改动后放弃恢复却返回 true。**按 0/4 的命中率，这四条在动手前务必自己核。**

## 四、功能冲突

### 先更正我自己的一条错误

我第一版说 `FgBoost` × `QuantumTweak` 是真冲突、会把 `Win32PrioritySeparation` 卡在中间态。**这条是错的。**

`QuantumTweak.cs:131` 的 `Repair()` 在写自己的值之前显式做了 `if (FgBoost.HasResidue()) FgBoost.Restore()`——先还原退役壳残留再写自己的，LIFO 正确，冲突早就协调过了。我只看了两个快照槽各自独立就下结论，没往上翻调用链。四弟的定性准确：属于「已知冲突、靠调用顺序维系」，真实风险只在将来新增绕过 `Repair()` 的清除路径时才会破。

### 中断亲和两代台账在清除路径上顺序反转［三支柱已验证］

涉及 `RetiredIrqAffinity`（`IrqAff_` 前缀）、`IrqRelocate`（`IrqReloc_` 前缀）、`LegacyPurge`、`IrqAffinityEngine`，写同一组值：`Enum\<设备>\Device Parameters\Interrupt Management\Affinity Policy` 下的 `DevicePolicy` + `AssignmentSetOverride`。

- **支柱一**：`OwnedByOtherTweaks()`（`IrqRelocate.cs:199`）函数体只查 `NetworkAffinityTweak.EnabledByPavise`，三个退役台账一个都不查 — 属实
- **支柱二**：`LegacyPurge` 里 `RetiredIrqAffinity.Disable` 排在 `IrqRelocate.Revert` **之前**，不是 LIFO — 属实
- **支柱三**：两者写同一组值 — 属实

**失效链**：退役残留清理曾失败（典型触发条件是启动时没管理员权限，写 `Enum` 设备键被拒）→ 用户在中断页钉了同一台设备，`IrqReloc_` 快照把残留值当「原值」存下 → 走一键清除，退役壳先还原写回真原值，紧接着 `IrqRelocate.Revert` 用过期快照把掩码又写回旧残留值。**两套备份同时消耗完，设备上永久留下退役功能的掩码，再没有任何备份能还原它。**

**本机实测：链条不完整。** 只有在役的 `IrqReloc_*`（钉在 NVIDIA USB Type-C Port Policy Controller 上，`DevicePolicy=4`、掩码 `0x0C00`），没有任何 `IrqAff_`/`UsbAff_`/`StorAff_` 退役台账。这台机器触发不了，但代码路径是通的。

**修法**：`OwnedByOtherTweaks()` 纳入三个退役台账；`LegacyPurge` 把 `IrqRelocate.Revert` 提到退役壳之前；迁移在 `IrqRelocate.HasResidue` 为真时延迟清理。

### `IfeoBoost` × `CfgOffTweak` — 共键、键级删除窗口［四弟报告，未独立验证］

同一个 `IFEO\<exe>` 键，由同一个 Boost 循环对同一渲染器调用。值不撞（`PerfOptions` vs `MitigationOptions`），各自维护「这个键是不是我建的」标记（`IfeoMk_` 2 位 vs `CfgMk_` 1 位），Restore 时都可能删整个键。当前靠空键检查兜底不会误删，最坏只留一个空残留键；真正的风险是将来出现第三个 IFEO 写入方时两套标记互相误判。另外 `NormalizeExe` 行为不一致：IfeoBoost 不剥路径，CfgOff 剥。

### `MsiModeTweak` × 中断亲和 — 顺序敏感但不撞键［四弟报告］

同一父键下一个写 `MSISupported`，一个写 `Affinity Policy`。MSI 开关改变中断投递方式，与已钉住的亲和掩码同时生效时行为互相影响。`IrqAffinityEngine.ReportMsiState` 只记日志不拦截。

### 已排雷（不冲突）

四弟排掉 7 组「疑似同键但实际不撞」：QoS 前缀不同、bcdedit 值名不同、服务集互斥、`NvGlobalTweaks` 与 `NvDrsTweaks` 快照域不同、`Mmcss` 与 `NetTweak` 值名不同等。

---

## 五、可以整合的地方

> **处置**：六项做了五项，每项落地后都跑完整自测确认 257 项全绿。
>
> - **1 IfeoBoost + CfgOffTweak** → 新增 `IfeoStore`，名单／键存在性标记／空键清理／exe 名规范化收成一份。顺带修掉两边 `NormalizeExe` 不一致（IfeoBoost 原本不剥路径，同一个进程名两边会算出不同键名），统一取剥路径那版。
> - **2 SvcPause + UpdatePause** → 新增 `ServicePauser` 参数化引擎，"先落盘意图再动手、落盘失败立刻回滚"这套崩溃续还原的次序只留一份实现；两边差异（服务名、落盘键、是否跳过没在跑的服务）降为构造参数，日志文案留在各自文件。
> - **3 三个退役亲和壳** → 收敛到 `RetiredAffinityLedgers`，同时是第四节冲突修复的数据源。
> - **4 教程修复家族抽契约** → 新增 `RepairCatalog`，五件套同构的 5 项进目录，`AutoApply` 标志位决定谁开机自动执行，`AutoRepair` 改为遍历目录。（四弟把 `NagleTweak`/`DisplayGuard` 也列进这一族，核查后它们只有 `HasResidue`/`Restore`，不同构，未收。）
> - **5 GameExeTweaks 字段工具搬家** → 新增 `PrefFieldText`，在役的 `WindowedOptTweak` 4 处引用改为直连；退役壳只留转发，将来确认线上无残留即可整个删除。
> - **6 DevicePowerTweak + HidPowerTweak** → **未做**。两者只是模式相似（Scan/Enable/Restore），实现完全不同：一个改网卡 `PnPCapabilities` 位，一个处理 USB 选择性挂起还要分 DWORD/Binary 两种值类型。合并会造出满是分支的类，比现在两个清晰的类更差。

最大一块是退役壳：**14 个文件 965 行，占 49 个 tweak 文件的 28%**，全部只剩 `HasResidue` / `Restore`。

| # | 合并对象 | 理由 |
|---|---|---|
| 1 | `IfeoBoost` + `CfgOffTweak` | 重复度最高（名单／标记／空键清理／NormalizeExe 约 90% 同构）且同循环调用，合并顺带消掉标记不对称 |
| 2 | `SvcPause` + `UpdatePause` + `DoTweak` 服务部分 | 骨架几乎逐行同构，只差服务名数组 |
| 3 | 三个退役亲和壳 | 完全相同的引擎包装只差前缀字符串；这张表正好当冲突修复里 `OwnedByOtherTweaks` 的数据源 |
| 4 | 「教程修复」家族抽 `IRepairTweak` | Quantum／Net／PlatformClock／Fth／Nagle／DisplayGuard 全是同一形态。**和第二节是同一件事的两面**——抽了契约之后转自动执行只是加个标志位 |
| 5 | `GameExeTweaks` 字段工具搬家 | 见下方警告，这是合并退役壳的前置条件 |
| 6 | `DevicePowerTweak` + `HidPowerTweak` | 网卡省电和键鼠 USB 省电是同一件事的两半，可并成一个「外设省电抑制」开关 |

**`GameExeTweaks` 不能直接删**［已验证］ — `WindowedOptTweak.cs` 有 4 处（40、64、89、90 行）调用它的 `ReadField` / `MergeField` / `RemoveField` / `RestoreField`。这是唯一一个「退役壳里长着在役代码」的实例，得先把字段工具搬出来，那个壳才能按退役流程退场。

**完整的 14 个退役壳**：`DisplayGuard` `FgBoost` `GameExeTweaks` `MpoTweak` `NagleTweak` `Notif` `NvGlobalTweaks` `PowerPlan.CorePark` `RetiredIrqAffinity` `StorageAffinityTweak` `SvcYield` `UploadYield` `UsbInterruptAffinityTweak` `VisualFx`

---

## 六、边界，和我没做的事

**台架是合成负载，不是真实游戏** — 已用新写的 `tools/D3DLab` 在真 D3D11 呈现路径上复核过（并有 PresentMon ETW 独立取证），GDI 这条边界已消除；但两者仍都是合成负载。真游戏的判据在 `tools/bench`（PresentMon + 真实场景 ABAB 协议），那需要你自己开游戏跑。原 StressLab 渲染器是 GDI 假游戏，压力源是 10 线程狂扫 64MB，比真实后台狠（README 里作者自己也标了这条边界）。挤核那三轮数据方向极其一致，但绝对值不能当游戏帧率承诺。

**测试期间机器上有 7~10 个 node 进程** — 四个模型在同时读代码。两臂受同等污染，A/B 对比有效，但基线偏低，这也是测出的 +76% 小于 README 里 +186% 的原因之一。

**三弟标的两个「加条件」项没能实测**［未验证］ — `IdleStateTweak`（笔记本禁 C-state）和 `GpuPowerMax`（Max-Q 共享功耗墙挤 CPU 预算）。这两个要真游戏 + HWiNFO 录 CPU/GPU 功耗与频率曲线才能判。判据：开启后若 CPU 频率中位数下降或 avg FPS 下降即证伪。

**一个安全线索，可能与 Pavise 无关**［实测］ — 本机 `FeatureSettingsOverride=0x3` 且 `FeatureSettingsOverrideMask=0x3`，推测执行缓解处于关闭状态。但 `HKCU\Software\Pavise` 里没有任何 SpecMit 快照，说明不是 Pavise 关的。后果是 Pavise 现在也无法把它还原回安全状态。建议确认这是手动关的还是别的工具留下的。

---

## 附：四个模型的命中情况

| 模型 | 分工 | 耗时 | 命中 |
|---|---|---|---|
| deepseek-v4-pro | Bug 猎手 | ~20 min | 抽验 4 条 **2 真 2 误报**，行号常错但问题模式对 |
| glm-5.2 | 负优化审查 | ~8 min | 第一版全部「保持现状」太顺从；加「给出证伪方法」要求后第二版质量跳升，MsiMode 是 no-op 的预测**实测命中** |
| qwen3.8-max | 冲突与整合 | ~26 min | **最扎实**。建键矩阵再逐对精读，三根支柱全部验证为真，还**纠正了我一条错误结论** |
| kimi-k2.6 | 自动执行候选 | ~2 min | 快且判断稳，FthTweak 有权衡这条抓得准；但「NetTweak 改回 10 是纯收益」下得太轻 |

方法上的教训：**让模型给出「怎么实测证伪你自己的结论」，比直接问结论有用得多。** glm 两版的差距完全来自这一句提示词。

---

*全部结论基于 2026-08-20 在 i7-9750H / RTX 2070 Max-Q / Windows 11 26200 上的实测。台架脚本、原始输出与四个模型的完整回复在会话 scratchpad 里。代码一行未动。*

---

## 本次改动的文件

| 文件 | 改动 |
|---|---|
| `src/Platform/CpuPartitionPolicy.cs` | 门槛 7 → 5，注释换成三轮实测数据 |
| `src/Core/VersionMigrations.cs` | 新增恢复迁移；三条退役亲和清理加 `!IrqRelocate.HasResidue` 闸门 |
| `src/Core/LegacyPurge.cs` | 中断亲和清除改 LIFO 顺序 |
| `src/Core/Suppression/IrqRelocate.cs` | `OwnedByOtherTweaks()` 纳入三代退役台账 |
| `src/Core/Tweaks/RetiredAffinityLedgers.cs` | **新增**，三代退役台账统一表 |
| `src/Core/Tweaks/AutoRepair.cs` | **新增**，开机自动校正被改坏的系统默认值 |
| `src/Core/Tweaks/RetiredIrqAffinity.cs` 等三个壳 | 引擎来源改为统一表 |
| `src/Core/Tweaks/NvDrsTweaks.cs` | 驱动不可用而显卡在场时点明原因再返回 |
| `src/Core/Tweaks/WlanGuard.cs` | 把实测结论写进注释，防止后人误「修」 |
| `src/Program.cs` | 启动维护序列挂上 `AutoRepair.Run()` |
| `src/Platform/Lang.cs` | 改写被推翻的 `gm.squeezebg.smallcpu` 文案；新增 4 条 key |
| `src/Ui/Pages/PanelForm.GameConfigPage.cs` | 不支持时的说明文案跟随新门槛 |
| `src/Core/Tweaks/IfeoStore.cs` | **新增**，IFEO 逐 exe 键的共享账（名单／标记／空键清理／exe 名规范化） |
| `src/Core/Tweaks/IfeoBoost.cs`、`CfgOffTweak.cs` | 改为共用 `IfeoStore`，统一 `NormalizeExe` 行为 |
| `src/Core/Tweaks/ServicePauser.cs` | **新增**，服务暂停的参数化引擎 |
| `src/Core/Tweaks/SvcPause.cs`、`UpdatePause.cs` | 改为共用引擎，只留各自名单与日志 |
| `src/Core/Tweaks/RepairCatalog.cs` | **新增**，教程修复家族的统一目录与自动执行门槛 |
| `src/Core/Tweaks/PrefFieldText.cs` | **新增**，`键=值;` 串工具，搬出退役壳 |
| `src/Core/Tweaks/GameExeTweaks.cs` | 字段工具改为转发 |
| `src/Core/Tweaks/WindowedOptTweak.cs` | 4 处引用改为直连 `PrefFieldText`，不再穿过退役壳 |
| `tests/SelfTests.cs`、`tests/SelfTests.CoreMask.cs` | 期望值更新到新门槛，补 5 核边界与 4 核下限用例 |
| `tools/D3DLab/` | **新增台架**：真 D3D11 + DXGI 的假游戏、A/B 编排脚本、实测记录。C# P/Invoke 实现，`csc` 即可编译，不需要 C++ 工具链 |

## 复核证据

- 编译：主程序、自测构建均 `EXIT=0`
- 自测：**257 项 / 248 PASS / 0 FAIL / 9 SKIP**
- 反射复核本机实值：`SqueezeMinPhysical=5`、`SqueezeSupported=True`、`BackgroundSqueezeMask=0xC00`、`SqueezeStatusFor=Ok`
- 迁移复核：`GmSqueezeBg` 由 `0` 恢复为 `1`，`Restore_GmSqueezeBg_1812` 标记已置
- 提权端到端：`NetworkThrottlingIndex` 由 `0xFFFFFFFF` 自动校正为 `0xA`，快照 `PrevNetThrottle==-1=10` 完整、可撤销
- 幂等复核：修好后再查 `NetTweak.NeedsRepair()=False`；`QuantumTweak` 本机 `0x26` 已是最大档 `NeedsRepair()=False`，自动执行不会误动
- 目录复核：`RepairCatalog.All=5 项`，`Auto()=net, inputmyth, quantum`
- 五轮合并每轮落地后单独跑全套自测，均 257 项 / 0 FAIL
- WLAN 实测探针：媒体流模式随句柄关闭自动失效（同句柄读回 1 → 关闭 → 新句柄读回 0）
- D3D 呈现路径复核：真 D3D11 + DXGI + Present，挤核 +126%，PresentMon ETW 与渲染器自测两路互证差 < 0.4%
